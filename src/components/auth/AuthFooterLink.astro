---
import { cn } from "@/lib/utils";

interface Props {
  href: string;
  class?: string;
}

const { href, class: className } = Astro.props;
---

<a href={href} class:list={[cn("text-purple-300 transition-colors hover:text-purple-200 hover:underline", className)]}>
  <slot />
</a>
